name: signer
description: A signer to sign and verify raw data
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  hex: ^0.2.0
  polkadart_keyring: ^0.7.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
  coverage: ^1.11.1

scripts:
  test: --coverage=coverage
